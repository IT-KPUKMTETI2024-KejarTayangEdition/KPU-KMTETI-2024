import {Inter, Hammersmith_One} from "next/font/google";
import localFont from 'next/font/local'

export const pixelgamer = localFont({src: "./PixelGamer.otf"});
export const hammersmith = Hammersmith_One({weight: "400", subsets: ["latin"]});